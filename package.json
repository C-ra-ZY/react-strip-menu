{
  "name": "react-strip-menu-monorepo",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "bun run --filter react-strip-menu build",
    "lint": "bun run --filter react-strip-menu lint",
    "test": "bun run --filter react-strip-menu test",
    "test:watch": "bun run --filter react-strip-menu test:watch",
    "dev": "bun run --filter react-strip-menu-demo dev"
  }
}
